# Security: require packages to be published for at least 7 days
# before they can be installed. Mitigates supply chain attacks
# (e.g., compromised maintainer accounts publishing malicious versions).
# Requires npm >= 11.10.0
min-release-age=7
